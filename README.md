# clean_DF
clean function to clean DataFrames; that handles the null values, removes the duplicates, standardise the column names to snake case &amp; return a clean DataFrame

#Python Code
Clean_DF.py a python code to clean dataframes. A reusable function.
